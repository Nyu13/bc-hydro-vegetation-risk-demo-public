# Streamlit Cloud: paste into App settings → Secrets (or copy to .streamlit/secrets.toml locally).
# BC Hydro HTTPS often fails Python certificate verification (e.g. Missing Authority Key Identifier).
BC_HYDRO_SSL_VERIFY = "0"

# Optional: force fully local demo data (no public HTTP).
# DEMO_OFFLINE_MODE = "1"
